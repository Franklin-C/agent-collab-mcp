{
  "name": "@franklineh/agent-collab-mcp",
  "version": "0.3.0",
  "description": "Connect a coding agent to an Agent Collab project: writes MCP config for Claude Code, Codex, Cursor, Gemini CLI, Google Antigravity, Grok Build, Muse Code, VS Code and Windsurf, or bridges stdio-only clients to the remote MCP endpoint.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-collab-mcp": "bin/agent-collab-mcp.mjs"
  },
  "files": [
    "bin"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mcp-remote": "0.8.3"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "private": false,
  "overrides": {
    "qs": "^6.16.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Franklin-C/agent-collab-mcp.git"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org",
    "provenance": true
  }
}
